import React from 'react';
import ReactDOM from 'react-dom';

import App from './components/App'; // Import main component

ReactDOM.render(<App />, document.getElementById('app')); // Main component's generated HTML should be put on the page (in the DOM)
